spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/good-hamburguer
    username: postgres
    password: aluno
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
        format_sql: true
    show-sql: true

  flyway:
    enabled: true
    url: jdbc:postgresql://localhost:5432/good-hamburguer
    user: postgres
    password: aluno